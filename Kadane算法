/*()kadane算法
编写一个 C 程序，输入一列整数，输出其中和最大的连续子列的和。
【 输入】第一行一个正整数 N，表示这列数的个数；第二行一列共 N 个整数
【 输出】和最大的连续子列的和*/
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    int *a,n,i,j,k;
    int cmax,max;
    printf("请输入正整数的个数：");
    scanf("%d",&n);
    a=(int *)malloc(sizeof(int)*n);
    if(a==NULL){
        printf("Faliure");
        return 1;
    }//判断是否malloc申请成功
    printf("请输入n个整数:\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",a+i);
    }
    cmax=max=*a;
    for(i=0;i<n;i++)
    {
        cmax=(cmax+a[i]>a[i])?(cmax+a[i]):a[i];
        max=(cmax>max)?cmax:max;
    }
    printf("最大的连续子列和:%d\n",max);
    free(a);//记得要free
    return 0;
}//算法本质是判断cmax(current_max)是否为负，如果是负数的话，就抛弃，从下一个开始就行了。
//全是负数的情况单独考虑
